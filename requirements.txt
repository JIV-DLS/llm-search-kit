# Run-time dependencies for llm-search-kit.
#
# This is the equivalent of pyproject.toml's [project].dependencies,
# kept here so contributors who prefer the classic
#   pip install -r requirements.txt
# workflow can install the kit without using setuptools editable mode.
#
# For development (pytest, flask example server), install
# requirements-dev.txt as well.

httpx>=0.25
pydantic>=2.0
python-dotenv>=1.0
